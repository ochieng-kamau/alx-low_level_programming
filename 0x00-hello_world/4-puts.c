#include <stdio.h>
/**
 * * main will print out a string to stdout
 * * 0 will be returned if successful
 */
int main(void)
	{
		puts("\"Programming is like building a multilingual puzzle");
		return (0);

	}
