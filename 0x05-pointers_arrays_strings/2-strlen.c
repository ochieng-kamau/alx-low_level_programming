#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: pointer to be updated
 * Return: 0 (Success)
 */

int _strlen(char *s)
{
	int a;

	for (a = 0; s[i] != '\0'; ++a)
	{
		continue;
	}
	printf("Length of the string: %d", a);

	return (0);
}
